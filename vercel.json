{
    "version": 1,
    "builds": [
        {
            "src": "server/build/server.js",
            "use": "@now/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}