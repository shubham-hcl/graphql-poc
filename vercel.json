{
    "version": 2,
    "rewrites": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ],
    "builds": [
        {
            "src": "server/build/server.js",
            "use": "@now/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server/build/server.js"
        }
    ]
}